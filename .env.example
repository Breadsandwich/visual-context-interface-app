# Required for Claude Vision analysis (screenshot + upload image analysis)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Override target app for iframe proxy
# TARGET_HOST=host.docker.internal
# TARGET_PORT=3000
